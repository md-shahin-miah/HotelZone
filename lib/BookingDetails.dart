class BookingDetails{

  var  hotelName;
  var location;
  var rating;
  var duration;
  var bookingStatus;

  BookingDetails(this.hotelName, this.location, this.rating, this.duration, this.bookingStatus);
}